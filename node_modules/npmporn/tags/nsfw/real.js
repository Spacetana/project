const requestHanlder = require("../handler");

class Real {
    ass() {
        let subreddits = ["ass", "paag", "asstastic", "buttplug", "whooties", "AssholeBehindThong", "Frogbutt", "rearpussy", "CuteLittleButts", "HungryButts", "reversecowgirl",
            "facedownassup", "butt", "butts", "pawg", "bigasses", "cosplaybutts", "girlsinyogapants", "BubbleButts", "assinthong", "smalltitsbigass", "CelebrityButts", "booty"
        ];
        return requestHanlder.makeRequest("reddit", subreddits[Math.floor(Math.random() * subreddits.lendth)]);
    }
    realgirls() {
        let subreddits = ["realgirls", "selfpix"];
        return requestHanlder.makeRequest("reddit", subreddits[Math.floor(Math.random() * subreddits.length)]);
    }
    beachgirls() {
        let subreddits = ["beachgirls"];
        return requestHanlder.makeRequest("reddit", subreddits[Math.floor(Math.random() * subreddits.length)]);
    }
}

module.exports = new Real();